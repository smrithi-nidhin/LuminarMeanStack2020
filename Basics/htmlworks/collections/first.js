
//arrays

//no size limit

var names=["mumbai","delhi","kerala"];
console.log(names[0]);


//for adding new element to array we can use push


names.push("CSK");
console.log(names);


//for deleting an object from an array we can use pop

names.pop();
console.log(names);

names.push(1500);  //we can store diffrent types of objects
console.log(names);



var numbers=[10,11,12,13,14,15];
console.log(numbers);


for(elements of numbers)
{
    console.log(elements);
}